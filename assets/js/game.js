var playerName = window.prompt("What's your robot's name?");
// Note the lack of quotation amrks around playerName window.alert(playerName);

// this creates a function named "fight"

function fight() {
    window.alert("The fight has begun!");
}

// fight ();